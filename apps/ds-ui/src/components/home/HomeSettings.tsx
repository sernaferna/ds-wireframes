import React from 'react';
import { Form } from 'react-bootstrap';

/**
 * Settings for the **Home** section of the application.
 */
export const HomeSettings = () => {
  return <Form.Text>No Home settings</Form.Text>;
};
