import React from 'react';
import { Form } from 'react-bootstrap';

/**
 * Settings component for the **Do** section.
 */
export const DoPageSettings = () => {
  return <Form.Text>No settings for Do page</Form.Text>;
};
